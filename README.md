# Graphics
This program is used to draw graphics from `.CSV` files.
Before using it, install its requirements.
```bash
mkdir ./graphics
git clone https://github.com/Haozhe-py/graphics.git
cd ./graphics
pip install -r requirements.txt
```
Then you can use this program in your command-line.
